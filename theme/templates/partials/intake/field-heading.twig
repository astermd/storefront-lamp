<h2 class="font-heading text-center text-[24px] font-bold leading-tight sm:text-[28px] text-heading">{{ field.label }}</h2>
{% if field.description %}
  <p class="mx-auto mt-3 max-w-[480px] text-center text-sm leading-relaxed text-muted">{{ field.description }}</p>
{% endif %}
