{% if field.image_url %}
  <img src="{{ field.image_url }}" alt="{{ field.label }}" class="mx-auto max-w-full rounded-sm{% if field.runtime_class %} {{ field.runtime_class }}{% endif %}" />
{% endif %}
