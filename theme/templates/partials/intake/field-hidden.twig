{# Rendered but not displayed ([10.11]), so a value the form computes still
   reaches the server as an ordinary answer. #}
<input type="hidden" id="intake-{{ field.name }}" name="{{ field.name }}" value="{{ field.value }}" />
