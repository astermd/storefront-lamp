<header class="text-center">
  <h1 class="font-heading text-[24px] font-bold leading-tight sm:text-[28px] text-heading">{{ field.label }}</h1>
  {% if field.description %}
    <p class="mx-auto mt-3 max-w-[480px] text-sm leading-relaxed text-muted">{{ field.description }}</p>
  {% endif %}
</header>
